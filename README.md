# openvr-glib

A glib wrapper and utilities for the OpenVR API

## Compilation

Configure the project
```
$ meson build
```

Build the project:
```
$ ninja -C build
```

Run the tests:
```
ninja -C build test
```